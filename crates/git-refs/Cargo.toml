[package]
name = "git-refs"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
git-core = { path = "../git-core" }
git-formats = { path = "../git-formats" }

[lints]
workspace = true
