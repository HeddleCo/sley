[package]
name = "sley-index"
version = "0.0.1"
description = "Native-Rust reader and writer for Git's index (staging area) file format, versions 2 through 4 plus the cache-tree extension."
repository.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
sley-core = { path = "../sley-core", version = "0.0.1" }

[lints]
workspace = true
