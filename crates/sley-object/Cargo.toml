[package]
name = "sley-object"
version.workspace = true
description = "Git's object model for the sley engine: commits, trees, tags, and the loose-object framing they share."
repository.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
# Identity resolution (`ident.c`) reads `user.name`/`user.email`/role overrides
# from the effective config, so object carries a config edge. Acyclic: config
# depends only on core + pathspec.
sley-config.workspace = true
sley-core.workspace = true

[lints]
workspace = true
