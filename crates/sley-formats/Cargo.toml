[package]
name = "sley-formats"
version = "0.0.2"
description = "Native-Rust readers and writers for Git's on-disk and wire formats: reftables, commit-graph, bundles, and repository layout."
repository.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
sley-core = { path = "../sley-core", version = "0.0.2" }
sley-config = { path = "../sley-config", version = "0.0.2" }
sley-index = { path = "../sley-index", version = "0.0.2" }
sley-object = { path = "../sley-object", version = "0.0.2" }

[lints]
workspace = true

