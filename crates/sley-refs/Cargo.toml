[package]
name = "sley-refs"
version = "0.0.1"
description = "Native-Rust Git reference store for the sley engine: loose refs, packed-refs, reftables, and reflogs."
repository.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
sley-config = { path = "../sley-config", version = "0.0.1" }
sley-core = { path = "../sley-core", version = "0.0.1" }
sley-formats = { path = "../sley-formats", version = "0.0.1" }

[lints]
workspace = true
